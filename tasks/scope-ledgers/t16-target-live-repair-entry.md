# T16 Target Live Repair Entry

## authorized

- Add a credentialed target repair/escalation live entry that reuses the
  retained Linear Project/Team configuration, Conductor Project Label, isolated
  run scope, Git fixture, and production Podium/Conductor boundary.
- Create one external Root, process its exact approval actions, and return the
  projected durable convergence escalation facts after cleanup.

## required_consequences

- The live entry creates no workflow records outside the external Root and
  plain Human response boundary delegated to the target runner.
- Child process environment remains secret-free; credentials cross only the
  existing approved setup/profile boundaries.
- Cleanup runs after success or failure, and missing credentials remain
  `unverified` at the CLI boundary.

## out_of_scope

- Credentialed acceptance in the current environment, restart recovery,
  delivery, scheduling, final all-scenario verdict assembly, CI/Makefile
  cutover, or removal of `core-live`.
- Automatic cleanup of retained Linear Roots or Project Labels.
- New workflow persistence, queue, checkpoint, or provider-context behavior.

## assumptions_requiring_approval

None.

## deferred_ideas

- Add retained real-boundary evidence to the final target scenario verdict once
  credentials are available.
