[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "performer-api"
version = "0.1.0"
description = "Shared contracts for the Symphony Podium, Conductor, and Performer roles."
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
