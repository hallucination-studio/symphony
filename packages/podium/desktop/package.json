{
  "name": "podium-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:sidecar": "../../../.venv/bin/python ../../../tools/build_desktop_sidecars.py",
    "dev": "npm run build:sidecar && tauri dev",
    "build": "npm run build:sidecar && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
